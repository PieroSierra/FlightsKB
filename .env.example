# Flights KB Configuration

# API Key for protected endpoints (rebuild, ingest)
# Generate a random string for your deployment
FLIGHTSKB_API_KEY=your-secret-api-key-here

# Optional: Override default paths
# FLIGHTSKB_KNOWLEDGE_DIR=./knowledge
# FLIGHTSKB_INDEX_DIR=./index

# Optional: Embedding model (default: all-MiniLM-L6-v2)
# FLIGHTSKB_EMBEDDING_MODEL=all-MiniLM-L6-v2

# Optional: CORS origins (comma-separated)
# Default includes localhost and GitHub Pages
# FLIGHTSKB_CORS_ORIGINS=http://localhost:5173,https://your-app.onrender.com

# =============================================================================
# GitHub Integration (Required for Render deployment)
# =============================================================================
# Fine-grained Personal Access Token with Contents read/write permission
# Create at: GitHub → Settings → Developer settings → Personal access tokens → Fine-grained tokens
GITHUB_TOKEN=github_pat_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Repository owner (your GitHub username or organization)
GITHUB_OWNER=your-github-username

# Repository name
GITHUB_REPO=FlightsKB

# Branch for commits (default: main)
# GITHUB_BRANCH=main
